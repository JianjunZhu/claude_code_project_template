# PAPER_NOTES.md —— 论文写作笔记

> 用途：将论文中每一条 claim、每一张表、每一幅图，反向追溯到具体的 artifact 与证据等级，确保"写进论文的内容都能被复现与核验"。
> 适用对象：撰稿人、合作者、审稿前自检。
> 本文件是**空模板**：除"本模板于 2026-06-19 创建"外，不含任何真实项目事实。所有具体内容均为占位符或 PENDING，须由真实 artifact 替换后方可引用。

---

## 0. 文档边界（先读这一节）

- 本文件只记录**面向论文的 claim 与证据映射**，不替代以下文档：
  - Agent 行为规则 → `CLAUDE.md`
  - 当前项目事实 → `docs/PROJECT.md`
  - 科研证据与实验流水 → `docs/EVIDENCE.md`、`docs/EXPERIMENT_LOG.md`
  - 长期经验 → `MEMORY.md`
- 论文中任何数字/结论，必须能在 `docs/EVIDENCE.md` 找到对应 artifact 记录；本文件提供"论文视角"的索引层。
- 在没有真实 artifact 支持前，所有状态保持 PENDING / NOT VERIFIED / INCOMPLETE / TBD。

### 统一证据等级（低 → 高，全模板共用同一套）

| 等级 | 含义 |
|---|---|
| 1 | 假设 |
| 2 | 设计方案 |
| 3 | 实现正确性检查 |
| 4 | 开发集验证 |
| 5 | 内部验证 |
| 6 | 冻结内部测试 |
| 7 | 锁定 holdout 测试 |
| 8 | 外部验证 |
| 9 | 独立复现 |

> 纪律：不得把低等级证据描述成高等级。例如仅有开发集（等级 4）结果，不得在论文中表述为"在 holdout / 外部验证上成立"。

---

## 1. 论文元信息

| 项 | 值 |
|---|---|
| 论文工作标题 | `<论文标题>`（TBD） |
| 对应项目 | `<项目名称>`（TBD） |
| 任务类型 | `<任务类型>`（TBD） |
| 研究目标 | `<研究目标>`（TBD） |
| 目标期刊/会议 | `<目标期刊>`（TBD） |
| 主要指标 | `<主要指标>`（TBD） |
| 辅助指标 | `<辅助指标>`（TBD） |
| 当前阶段 | INCOMPLETE |
| 关联 commit hash | TBD |

---

## 2. 核心 claim → 证据映射表

> 规则：每条 claim 必须能回答——**结论是什么 / 由哪个 artifact 支持 / artifact 在哪 / 何时生成 / 由什么命令生成 / 是初步还是最终证据**。
> 未满足上述六问的 claim，状态一律 PENDING，不得写入正文。

| 编号 | 核心 claim（论文中的表述） | 支持证据 / artifact（路径或 ID） | 证据等级(1-9) | 生成时间 | 生成命令 | 状态 |
|---|---|---|---|---|---|---|
| C1 | `<核心 claim 1>` | `<artifact 路径/ID>`（TBD） | TBD | TBD | `<生成命令>`（TBD） | PENDING |
| C2 | `<核心 claim 2>` | `<artifact 路径/ID>`（TBD） | TBD | TBD | `<生成命令>`（TBD） | PENDING |
| C3 | `<核心 claim 3>` | `<artifact 路径/ID>`（TBD） | TBD | TBD | `<生成命令>`（TBD） | PENDING |

> 填写须知：
> - "证据等级"取该 claim 当前**最弱可追溯环节**对应的等级，而非最理想值。
> - "是初步还是最终证据"在状态列体现：PENDING（待验证）/ NOT VERIFIED（未确认）/ 等级达标且经核对后方可标为已支持。
> - 任一 claim 涉及 holdout / 外部测试，须确认遵守冻结测试纪律（见第 8 节）。

---

## 3. 表格 → artifact 映射表

> 论文里每张表的每个数字，都应能定位到唯一 artifact（如评估输出文件 + 行号/字段）。

| 表编号 | 表标题/内容 | 数据来源 artifact（路径/ID） | 生成脚本 | seed / 配置 | 证据等级 | 状态 |
|---|---|---|---|---|---|---|
| T1 | `<表1标题>` | `<artifact 路径/ID>`（TBD） | `<生成脚本>`（TBD） | seed=TBD / `<配置>` | TBD | PENDING |
| T2 | `<表2标题>` | `<artifact 路径/ID>`（TBD） | `<生成脚本>`（TBD） | seed=TBD / `<配置>` | TBD | PENDING |

> 填写须知：
> - 表中若含 baseline 或对比方法，须在第 7 节"方法创新与工程整合区分"中说明来源与是否为原文复现。
> - 聚合（aggregate）数值须能下钻到 case-level 明细 artifact；否则标 PENDING。

---

## 4. 图 → 源数据 / 生成脚本映射表

> 每幅图须可由"源数据 + 脚本 + 参数"重新生成；图片在仓库内使用相对路径引用。

| 图编号 | 图标题/内容 | 源数据 artifact（路径/ID） | 生成脚本 | 图片相对路径 | 证据等级 | 状态 |
|---|---|---|---|---|---|---|
| F1 | `<图1标题>` | `<源数据 路径/ID>`（TBD） | `<生成脚本>`（TBD） | `../outputs/<图1>.png`（TBD） | TBD | PENDING |
| F2 | `<图2标题>` | `<源数据 路径/ID>`（TBD） | `<生成脚本>`（TBD） | `../outputs/<图2>.png`（TBD） | TBD | PENDING |

**行内公式示例（VS Code 可渲染）**：指标 $m$ 定义为 $m = f(\hat{y}, y)$，其中 $\hat{y}$ 为预测、$y$ 为标签（占位，TBD）。

**块公式示例**（独立成段，前后留空行，不置于引用块内）：

$$
\mathrm{Metric}_{\text{agg}} = \frac{1}{N}\sum_{i=1}^{N} \mathrm{Metric}_{\text{case}}^{(i)}
$$

上式仅为格式占位，具体 $\mathrm{Metric}$ 与 $N$ 的真实值 TBD，未验证前为 NOT VERIFIED。

---

## 5. 局限性清单（Limitations）

> 论文应主动陈述局限；此处先以占位条目维护，定稿前逐条核实。

| 编号 | 局限性描述 | 影响范围 | 拟缓解 / 后续工作 | 状态 |
|---|---|---|---|---|
| L1 | `<局限性 1：如数据规模/分布>` | TBD | TBD | PENDING |
| L2 | `<局限性 2：如外部验证缺失>` | TBD | TBD | PENDING |
| L3 | `<局限性 3：如指标口径限制>` | TBD | TBD | PENDING |

> 安全表达提醒：在证据不足时，**不得**声称临床有效 / 诊断更优 / 泛化强 / 鲁棒 / state-of-the-art / 可临床使用；使用"我们假设 / 预期 / 仍需验证 / 当前证据仅支持……"等谨慎措辞。

---

## 6. 负结果记录（必须保留）

> 失败运行、被否定假设、数据泄漏风险、指标不一致等负结果**只可归档、不可删除**。论文相关的负结果在此登记，明细可链接到 `docs/EXPERIMENT_LOG.md` / `experiments/records_archive/`。

| 编号 | 负结果 / 否定假设 | 证据 artifact（路径/ID） | 发现时间 | 处理（归档位置） | 状态 |
|---|---|---|---|---|---|
| N1 | `<负结果 1>` | `<artifact 路径/ID>`（TBD） | TBD | `experiments/records_archive/`（TBD） | PENDING |
| N2 | `<负结果 2>` | `<artifact 路径/ID>`（TBD） | TBD | `experiments/records_archive/`（TBD） | PENDING |

---

## 7. 方法创新 vs 工程整合（明确区分）

> 论文需如实区分"真正的方法学创新"与"已有组件的工程整合/工程优化"，避免夸大贡献。

| 编号 | 条目 | 类别（方法创新 / 工程整合） | 已有工作出处 | 与既有工作的差异 | 证据等级 | 状态 |
|---|---|---|---|---|---|---|
| M1 | `<贡献点 1>` | TBD | `<已有出处>`（TBD） | TBD | TBD | PENDING |
| M2 | `<贡献点 2>` | TBD | `<已有出处>`（TBD） | TBD | TBD | PENDING |

> 填写须知：
> - 标为"方法创新"的条目，须有可追溯证据支撑其差异性与有效性，否则降级为工程整合或标 NOT VERIFIED。
> - 工程整合不贬义，但不得包装成方法学创新。

---

## 8. 冻结测试纪律核对（写作前自检）

- [ ] 未在 holdout / 外部测试集上调过 checkpoint / 阈值 / 架构 / loss（PENDING）
- [ ] 未反复使用 holdout / 外部测试集来优化方法（PENDING）
- [ ] 测试前已冻结：协议 / 脚本 / 模型 / 阈值 / 配置（PENDING）
- [ ] 任何潜在数据泄漏均已在 `docs/EXPERIMENT_LOG.md` 记录（PENDING）
- [ ] 论文中"外部验证 / holdout"表述与实际证据等级一致（PENDING）

---

## 9. 不得编造（红线提醒）

- **不得编造引用**：每条参考文献须真实存在、可核验；不得臆造作者、年份、期刊、DOI 或结论。
- **不得编造结果**：任何指标 / 数据规模 / 训练状态 / 运行时间 / 硬件占用，无真实 artifact 支持就标 PENDING / NOT VERIFIED，不得填入正文。
- **不得编造对比**：baseline 与对比方法的数值须注明来源（原文报告值 / 本工作复现值），并区分二者；复现值须有可追溯 artifact。
- **不得拔高证据等级**：开发集 ≠ 内部验证 ≠ holdout ≠ 外部验证 ≠ 独立复现。
- 任何尚未满足第 2 节"六问"的内容，留在 PENDING，不进入投稿稿。

---

## 10. 投稿前最终核对清单（占位）

- [ ] 第 2 节所有核心 claim 状态均已脱离 PENDING 且证据等级标注准确
- [ ] 第 3、4 节每个表/图均可由脚本 + 源数据复现，commit hash 已记录
- [ ] 第 5 节局限性完整、措辞谨慎，无超证据声明
- [ ] 第 6 节负结果已保留并归档
- [ ] 第 7 节贡献分类如实
- [ ] 第 9 节红线逐条通过
- [ ] 与 `docs/EVIDENCE.md`、`docs/RESULT_AUDIT.md` 对账一致

---

*本文件随论文进展持续追加；任何新增 claim / 表 / 图，先在本文件登记并标 PENDING，再回填证据。*
