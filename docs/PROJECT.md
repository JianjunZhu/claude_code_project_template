# PROJECT.md — 当前项目事实

> 本文件记录**项目事实**（当前真实状态），未知一律使用占位符，**不得猜测真实值**。
> 文档分类提醒：项目事实在此；Agent 行为规则见 `../CLAUDE.md`；科研证据见 `./EVIDENCE.md`、`./EXPERIMENT_LOG.md`；长期经验见 `../MEMORY.md`。
>
> 本模板于 2026-06-19 创建。除该创建日期外，本文件目前不含任何已确认的具体项目事实——全部为占位符，待项目实际推进后逐项填入并标注证据。

---

## 1. 基本信息

| 字段 | 值 |
| --- | --- |
| 项目名称 | `<项目名称>` |
| 任务类型 | `<任务类型>` |
| 研究目标 | `<研究目标>` |
| 创建日期 | 2026-06-19 |

## 2. 背景

`TBD`（项目背景待填写；在有真实依据前不得编造动机或既有结论。）

## 3. 数据集

> 数据/权重默认不入 git，以 manifest + checksum 替代；隐私路径不写入 git。详细应记录项见 `./RESEARCH_RULES.md` 第 6 节。

| 字段 | 值 |
| --- | --- |
| 数据集名称 | `<数据集名称>` |
| 数据路径 | `<数据路径>`（`TBD`） |
| 划分 train/validation/test | `TBD` |
| holdout | `TBD` |
| 外部测试集 | `<外部测试集>`（`TBD`） |
| 样本数 / 分布 | `TBD`（未知不猜） |
| manifest / checksum | `TBD` |

## 4. 指标

| 字段 | 值 |
| --- | --- |
| 主要指标 | `<主要指标>` |
| 辅助指标 | `<辅助指标>` |
| 失败 / 安全指标 | `TBD` |
| case-level / aggregate 口径 | `TBD` |

## 5. 运行环境

| 字段 | 值 |
| --- | --- |
| Python 解释器 | `<Python解释器>` |
| Conda 环境 | `<Conda环境>` |
| 依赖版本 | `TBD` |
| 硬件 | `TBD`（不编造硬件/占用） |

## 6. 代码与产物

| 字段 | 值 |
| --- | --- |
| 主脚本 | `<主脚本>` |
| 模型权重 | `<模型权重路径>`（`TBD`；默认不入 git） |
| 输出目录 | `<输出目录>` |
| commit hash（当前里程碑） | `TBD` |

## 7. 写作与资助

| 字段 | 值 |
| --- | --- |
| 目标期刊 | `<目标期刊>` |
| 基金项目类型 | `<基金项目类型>` |

## 8. 当前状态

- 模板已初始化（2026-06-19）。
- 代码：`INCOMPLETE`（尚无项目代码）。
- 数据：`PENDING`（尚未接入任何数据集）。
- 实验：`PENDING`（尚无任何运行或结果）。
- 结论：`NOT VERIFIED`（无任何已确认结论）。
- 整体证据等级：`TBD`（项目尚未产生 artifact，参见 `./RESEARCH_RULES.md` 第 0 节证据等级）。

## 9. 关联文档

- Agent 行为规则：`../CLAUDE.md`
- 长期经验：`../MEMORY.md`
- 任务简报：`./TASK_BRIEF.md`
- 科研纪律详规：`./RESEARCH_RULES.md`
- 证据台账：`./EVIDENCE.md`
- 实验流水：`./EXPERIMENT_LOG.md`
- 结果审计：`./RESULT_AUDIT.md`
- 论文记录：`./PAPER_NOTES.md`
- 模板变更日志：`./TEMPLATE_CHANGELOG.md`
